'use client';

export default function LoginProvidersSettingsPage() {
  return (
    <div>
      <h1 style={{ fontSize: '1.25rem', fontWeight: 600, color: 'var(--text-main)', marginBottom: '0.5rem' }}>Login Providers</h1>
      <p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Configure OAuth, SSO, and social login options.</p>
    </div>
  );
}
